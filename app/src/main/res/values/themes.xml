<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BroadcastReceiver" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="MyToastConnected">
        <item name="stTextColor">@color/white</item>
        <item name="stColorBackground">@color/green</item>
        <item name="stIconStart">@drawable/battery</item>
        <item name="stLength">LONG</item> LONG or SHORT
        <item name="stRadius">10dp</item>
        <item name="stStrokeColor">@color/white</item>
        <item name="stStrokeWidth">3dp</item>
    </style>


    <style name="MyToastDisconnected">
        <item name="stTextColor">@color/white</item>
        <item name="stColorBackground">@color/red</item>
        <item name="stIconStart">@drawable/ic_battery_off</item>
        <item name="stLength">LONG</item> LONG or SHORT
        <item name="stRadius">10dp</item>
        <item name="stStrokeColor">@color/white</item>
        <item name="stStrokeWidth">3dp</item>
    </style>

    <style name="MyCustomToast">
        <item name="stTextColor">@color/white</item>
        <item name="stColorBackground">@color/purple_500</item>
        <item name="stLength">LONG</item> LONG or SHORT
        <item name="stRadius">10dp</item>
        <item name="stStrokeColor">@color/white</item>
        <item name="stStrokeWidth">3dp</item>
    </style>
</resources>